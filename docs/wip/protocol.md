## Event Topics

- `agent/lifecycle.control` — request agent state changes (init/start/stop/etc.)
- `agent/lifecycle.status` — respond with current agent state
- `ping`, `pong` — health checks
- `agents` / `.reply` — discovery and registry introspection
