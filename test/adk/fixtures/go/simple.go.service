// Parity fixture: the Go side of TestCrossADKParity/DescribeMetadata.
package agent

import "context"

const (
	ID          = "parity_simple"
	Type        = "service"
	Version     = "1.0.0"
	Description = "Minimal service agent"
)

func Start(ctx context.Context) error {
	<-ctx.Done()
	return nil
}
