{"id": "GAPI-DIV-001", "surface": "flake.nix shellHook", "violation": "gopy installed via 'go install github.com/go-python/gopy@latest' - a floating tool version inside a hermetic shell", "rule": "ecosystem-manifesto section 12 class 1 (floating tool version in hermetic shell); operator-field-guide section 2 check 5 (binding toolchain pinned, not @latest)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: gopy pinned at v0.4.10, built from the in-repo tools/gopy module (x/tools lifted via replace for go1.26 compatibility); hooks build into $GOBIN so a host gopy cannot shadow the pin. Artifact: 'mage doctor' PASS binding-toolchain (verifies the embedded module version).", "opened": "2026-07-22"}
{"id": "GAPI-DIV-002", "surface": "Magefile.go Test target", "violation": "Test runs 'go test -v ./...' with no -race flag", "rule": "go-manifesto section 4 (Test = go test -race ./...); section 12 (tests run with -race)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: Test target runs 'go test -race ./...'; TestShort and Fuzz added alongside. Artifact: 'mage test' output shows -race invocation.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-003", "surface": "logging (cmd/gapid, core/*, internal/logging)", "violation": "Structured logging uses github.com/rs/zerolog rather than the stdlib slog default", "rule": "go-manifesto section 8 (slog by default; a third-party logger needs a measured benchmark reason)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: migrated to log/slog. New exported core/logging builds handlers from LoggingConfig (JSON default, text for 'console', optional lumberjack file sink; Loki still fails loudly as unimplemented) - exported so goblin reuses the same wiring. All call sites (zerolog chains, the logcore/logevent/logctx facade, stdlib log) log through LogAttrs with typed constructors from internal/logattr; the facade tree internal/logging was deleted (auditlog had zero callers - removed as dead code rather than ported). sloglint attr-only enforces the style in the shared gate. rs/zerolog awaits 'go mod tidy' (operator-gated) to leave go.mod. Artifacts: core/logging unit tests; 'mage lint' exit 0; full 'mage test' green under -race.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-004", "surface": "Magefile.go target set", "violation": "No Doctor target exists", "rule": "ecosystem-manifesto section 7 (every repo ships a doctor target); operator-field-guide section 2", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: Doctor target calls magelib.Doctor with the six field-guide checks, one line per check, nonzero exit on failure. Artifact: 'mage doctor' prints six PASS lines in gapi.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-005", "surface": "Magefile.go / CI lint gate", "violation": "No Vuln (govulncheck) or gosec target; golangci-lint runs without a pinned explicit linter set", "rule": "go-manifesto section 12 (gosec and govulncheck required in the same gate; pinned explicit linter set incl. errorlint, sloglint)", "owner": "kernel", "status": "open", "exit": "REOPENED 2026-07-29 (v0.1.0-proto2b review). This entry's SURFACE is 'Magefile.go / CI lint gate' and its exit's only artifact is 'mage lint' running locally. The CI half was never built: gapi's workflow runs mage build, mage vuln, mage python:build and a raw 'go test -v ./...' - no job invokes mage lint, so gofmt, the pinned golangci-lint set and gosec never gate a push. The gate exists and is good; nothing enforces it. PRIOR EXIT: RESOLVED 2026-07-27: Vuln target (govulncheck, loud offline skip) added; Lint routes through magelib.Lint = gofmt check + golangci-lint with the pinned set in magelib/.golangci.yml + gosec. Pre-existing findings the gate now surfaces are tracked in GAPI-DIV-019. Artifact: 'mage vuln' and 'mage lint' run the gate.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-006", "surface": "buf.yaml / Magefile.go Proto target", "violation": "buf.yaml configures lint (STANDARD) and breaking (FILE) but no mage target or CI gate ever invokes 'buf lint' or 'buf breaking'", "rule": "ecosystem-manifesto section 4 (buf breaking run in every repo's gate); go-manifesto section 6", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: Proto target routes through magelib.BufGenerate (buf generate, then buf lint, then buf breaking --against .git#ref=HEAD). The gate is wired; pre-existing schema-naming violations it surfaces are tracked in GAPI-DIV-018. Artifact: 'mage proto' output shows all three buf invocations.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-007", "surface": "Magefile.go Build target / repo root", "violation": "No version stamping in the mage Build (plain 'go build'); no root VERSION file; '-X main.version' exists only in nix/package.nix hardcoded to 0.1.0; core/version/version.go uses GAPIVersion, not main.version", "rule": "go-manifesto sections 4 and 14 (version resolution algorithm, VERSION file single source, ldflags at link time)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29 (completes the 2026-07-27 partial). nix/package.nix now stamps '-X github.com/goppydae/gapi/core/version.GAPIVersion=${version}', matching Magefile.go:23; the previous '-X main.version' targeted a variable that does not exist, so a nix-built binary silently kept the 'dev' placeholder from core/version/version.go:12. The package could not build at all until the same commit fixed it (see GAPI-DIV-033), so this half had never been exercised. Artifact, read off the built binary rather than asserted: 'nix build' of nix/package.nix now succeeds and 'result/bin/gapid version' reports 'Runtime Core: 0.1.0' where it would previously have reported 'dev'. Both binaries stamped. NOTE for the record: Go DDK, Python DDK and Protobuf Schema Hash still print 'dev'/'unknown' - those fields are declared in core/version/version.go but never injected by any build. That is a separate, smaller gap and is NOT covered by this entry, whose violation text concerned the version only. Prior exit text preserved: --- RESOLVED 2026-07-27: root VERSION file added (0.1.0); Build and Install stamp core/version.GAPIVersion (the repo's real injection point, covering both binaries) via magelib.Version() with the env>tag>VERSION>dev algorithm. Artifact: './bin/gapid version' reports 'Runtime Core: 0.1.0'.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-008", "surface": "go.mod / pkg/magelib CheckHermetic", "violation": "No 'toolchain' directive in go.mod; the two toolchain pins (flake, go.mod) are not asserted to agree", "rule": "go-manifesto section 5 (double-pin agreement); operator-field-guide section 3 row 10", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: go.mod carries 'go 1.26.0' + 'toolchain go1.26.5' (operator-authorized; pin converged with goblin and the flake in the same session); CheckHermetic and doctor check 2 assert the shell go version equals the directive. Artifact: 'mage doctor' PASS pin-agreement.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-009", "surface": "pkg/magelib/common.go CheckHermetic", "violation": "CheckHermetic only prints a warning when a tool resolves outside /nix/store; it does not fail closed", "rule": "go-manifesto section 4 / section 1 (fail closed at boundaries); ecosystem-manifesto section 7", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: CheckHermetic returns a non-nil error on any tool resolving outside /nix/store. Artifact: shadowing gcc with a non-store path turns 'mage doctor' red (FAIL hermetic-resolution, exit 1); restored afterward.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-010", "surface": "event topics (core/eventbus, core/supervisor)", "violation": "No exported topic constants exist; topics are inline string literals, and the well-known 'agent.network.running' topic named in the design docs is not defined or used anywhere in the repo", "rule": "ecosystem-manifesto section 3 (every cross-boundary topic is an exported constant in one kernel package)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28 (completes the 2026-07-27 partial): core/eventbus/topics.go now exports every cross-boundary topic - TopicAgentNetworkRunning plus the lifecycle family (Prefix, Action, Control, Status, Transition), each documented with its payload type and the Action/Control distinction spelled out. All non-test call sites (11 files incl. the ADK bridge) import the constants; zero inline cross-boundary literals remain. Artifact: grep for the literals returns only topics.go and tests; gates + suite green.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-011", "surface": "transport ALPN identifiers", "violation": "ALPN string 'gapi-quic' is an inline literal rather than a named constant governed by an append-only registry", "rule": "ecosystem-manifesto section 6 (ALPN registry; named constants; append-only with tombstones)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: core/transport/alpn.go exports ALPNGapiQUIC with the ecosystem ALPN registry named as its governing contract; kernel call sites import it and goblin consumes it via its own alpn.go (never redefined). Artifact: zero gapi-quic literals outside the constant definition; gates green.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-012", "surface": "flake.nix dev shell", "violation": "The gapi and goblin dev shells are hand-duplicated and only assumed identical; nothing asserts they agree", "rule": "ecosystem-manifesto section 11 (environment unification; copies asserted identical, not assumed)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: EnvCheck target (magelib.CheckShellUnification) compares real store paths of 9 shared tools across the gapi/goblin/magelib shells; all three flake.locks share one nixpkgs rev. The shared-nix-module refactor remains the recorded target (manifesto section 11 option 1). Artifact: 'mage envcheck' reports inventories agree across 3 shells.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-013", "surface": "Magefile.go target set", "violation": "No Fuzz or TestShort target exists", "rule": "go-manifesto sections 4 and 13 (Fuzz target per Fuzz* function; TestShort fast inner-loop subset)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: Fuzz target (magelib.FuzzAll, bounded -fuzztime 10s, loud no-op at zero targets) and TestShort (-short) added. Artifact: 'mage fuzz' and 'mage testshort' run.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-014", "surface": "releases", "violation": "No tagged releases yet; every build artifact is a workspace build built through the replace directive", "rule": "ecosystem-manifesto sections 8 and 12 item 5 (tagged releases pin tags and drop replace)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: first tagged release v0.1.0-proto2 pushed (magelib v0.1.0 dependency pinned from its published tag; replace directive dropped; sibling dev rides the committed go.work). The goppydae org is private: dev shells export GOPRIVATE, CI authenticates via the GOPPYDAE_READ_TOKEN secret. Exit artifact: the No-Replace Module Resolution CI job is green on the release line (run 30344000297) - a lone clone resolves the module graph from published tags with no replace, no siblings, no vendor.", "opened": "2026-07-22"}
{"id": "GAPI-DIV-015", "surface": "identity of events/agents (proto/{envelope,event,agent_status}.proto; core/eventbus/eventbus.go:33; core/lifecycle/controller.go:191)", "violation": "Envelope/Event/AgentStatus id fields are opaque strings, and event/lifecycle IDs are minted with UUIDv4 (uuid.New()), not UUIDv7 foreign keys threaded across logs/metrics/traces", "rule": "research/global-uuid-based-process-identity DDR-1 (UUIDv7) and DDR-3 (identity as foreign key)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: event and lifecycle-run ids minted via uuid.NewV7 (internal/ident) at both sites; ids remain strings on existing wire fields carrying UUIDv7 canonical text and thread through logs via logattr.EventID. (was: Mint via uuid.NewV7() from crypto/rand at these sites and carry the UUIDv7 as the foreign key on every event, log line, metric label, and trace span.)", "opened": "2026-07-22"}
{"id": "GAPI-DIV-016", "surface": "signal delivery (core/agentmgr)", "violation": "Local signal delivery has no start_epoch check and no pidfd_open/pidfd_send_signal guard, leaving the kernel-level PID-recycling ABA hazard open", "rule": "research/global-uuid-based-process-identity DDR-5 (start_epoch as cluster-level pidfd; end-to-end ABA safety)", "owner": "kernel", "status": "open", "exit": "REOPENED 2026-07-29 (v0.1.0-proto2b review). The stated SURFACE is 'signal delivery (core/agentmgr)' and the stated violation is that LOCAL signal delivery has no start_epoch check. core/procsig was built and is excellent, but core/agentmgr does not import it: go_agent.go:465 and :562 and python_agent.go:507 still call cmd.Process.Signal and cmd.Process.Kill by PID with no epoch guard. The only consumer of procsig is goblin (internal/supervisor/node_rpc.go, node_rpc_migration.go, supervisor.go), so the orchestrator-routed path IS ABA-safe while gapictl, the local supervisor and agent stop/reload are not. The hazard is closed on the cluster path and open on the local one - which is the half this entry names. PRIOR EXIT: RESOLVED 2026-07-28: core/procsig delivers via pidfd_open + pidfd_send_signal behind a check-pin-recheck start-epoch guard; typed ErrStaleEpoch (no retry) and ErrProcessGone; Identify() captures the locator (pid, start_epoch, pid_ns_inode); Linux-only by operator decision, non-Linux compiles and refuses (ErrUnsupported). (was: Record start_epoch per local process and, on signal, verify it then deliver via pidfd_open + pidfd_send_signal; reject a stale epoch with ErrStaleEpoch (no retry).)", "opened": "2026-07-22"}
{"id": "GAPI-DIV-017", "surface": "core/crypto", "violation": "Ed25519 signing/verification primitives exist but there is no capability-token verification codepath (rights bitmap, TTL, issuer key epoch) for the identity model", "rule": "research/global-uuid-based-process-identity DDR-9 (bearer Ed25519 capability tokens); ecosystem-manifesto section 8 (one verification codepath)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: core/crypto.VerifyCapabilityToken is the single verification codepath - Ed25519 over literal payload bytes, then validity window, then rights; typed ErrTokenMalformed/UnknownKey/Signature/Expired/Rights; SignCapabilityToken and RightForSignal live beside it; issuance stays orchestrator-side (goblin core/capability). (was: Provide the capability-token verification codepath in core/crypto (Ed25519 over the token fields, exp/issued/issuer_key_id checks); issuance stays orchestrator-side.)", "opened": "2026-07-22"}
{"id": "GAPI-DIV-018", "surface": "proto/ (all .proto files)", "violation": "buf lint STANDARD is red on the existing schemas: package 'gapi.v1.proto' lacks a version suffix and does not match its directory ('gapi/v1/proto' expected)", "rule": "go-manifesto section 6 (buf lint STANDARD); ecosystem-manifesto section 4 (schema governance)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28 (atomic cut-over, operator-approved: no release exists, goblin migrated in the same pass): sources moved to proto/gapi/v1 as package gapi.v1 (buf module rooted at proto/); generated code lands directly in pkg/proto via buf.gen module opt (go_package and Go imports unchanged); the duplicate proto/*.pb.go tree, the mirror step, and the zero-call-site gRPC Supervisor service (plus protoc-gen-go-grpc) are deleted; AGENT_STATE_UNSPECIFIED zero value and ACTION_ prefixes are number-preserving (wire-safe). Artifacts: buf lint zero findings; 'mage proto' green (breaking skipped once via PROTO_BREAKING_SKIP=1, logged; re-arms on first post-commit run); full 'mage test' green under -race incl. the cross-ADK suite (Any type_url change verified harmless end-to-end).", "opened": "2026-07-27"}
{"id": "GAPI-DIV-019", "surface": "adk/python/gapi/native (generated gopy/cgo output, checked in)", "violation": "Generated adk.go fails to compile under gcc 15 (C23 default makes 'typedef uint8_t bool;' illegal), surfaced by the go 1.26/nixpkgs toolchain bump; the broken package poisons './...' enumeration so 'mage lint' (typecheck), 'mage fuzz', and 'mage vuln' are red on it", "rule": "go-manifesto section 5 (the dev shell is the toolchain; a toolchain bump lands green); build-environment.md Python binding pipeline", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: CGO_CFLAGS=-std=gnu17 exported in the dev shells (both repos, symmetric) and -std=gnu17 added to the python:build gcc link step - the pinned gopy's generated C assumes pre-C23 'bool'; bindings regenerated via 'mage python:build' (now embed current python 3.14.6 store paths instead of the stale 3.13.9 ones). Artifact: 'go build ./...' green, full 'mage python:build' green, fuzz/vuln/lint all enumerate and run to completion. The gates now being red on OTHER debt is tracked separately: GAPI-DIV-022 (lint findings unmasked), GAPI-DIV-023 (called grpc CVE).", "opened": "2026-07-27"}
{"id": "GAPI-DIV-022", "surface": "lint gate (repo-wide, dominated by core/agentmgr)", "violation": "With the generated-bindings typecheck abort fixed (GAPI-DIV-019), golangci-lint analyzes the full tree for the first time and is red: 73 findings under the pinned set (mostly errcheck, e.g. unchecked socketFile.Close/agent.Stop in core/agentmgr)", "rule": "go-manifesto section 12 (pinned explicit linter set; carve-outs rule-level only)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: all 73 golangci findings fixed under the strict-propagation policy (operator-chosen): errors propagate through grown signatures, goroutine/callback termini log at error level, tests assert; dead code deleted; deprecated APIs migrated (local titleCase instead of promoting x/text); generated gopy bindings excluded rule-level in the shared config. Artifact: golangci-lint clean (0 findings) with the full 'mage test' suite still green. NOTE: with golangci passing, the gate reached gosec for the first time - that newly surfaced stratum is GAPI-DIV-024.", "opened": "2026-07-27"}
{"id": "GAPI-DIV-024", "surface": "gosec (repo-wide): 124 findings - 64 G103 unsafe (FFI/adk seam), 17 G304 file-by-variable, 14 G115 int conversions, 12 G204 subprocess, 9 G306 + 7 G301 permissions, 1 G703", "violation": "gosec had never actually run: the lint gate executes it after golangci-lint, which failed first from the day the gate was wired. With golangci clean the gate now reaches gosec and it is red", "rule": "go-manifesto section 12 (gosec required in the same gate; carve-outs rule-level and documented, never per-line)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: gate now skips generated code (magelib passes -exclude-generated; removed 73 findings in protoc-gen-go output and cgo intermediates). The 51 hand-written findings: G306/G301 modes tightened to 0600/0750 (16), G115 conversions bounds-checked (5), G703 fixed via os.UserHomeDir anchoring, all 17 G304 variable-path opens routed through the new audited internal/safeio chokepoint (clean + absolute, root-confined for cgroupfs and hash-dir walks; unit-tested). Rule-level carve-outs, justified at the Magefile call site: G204 (supervising agent processes is the product; discovery fenced, binaries signature-verified) and G304 (fires only inside safeio itself). Artifact: 'mage lint' exit 0 end-to-end (gofmt + golangci + gosec 0 issues); full 'mage test' green under -race (14 packages ok).", "opened": "2026-07-27"}
{"id": "GAPI-DIV-023", "surface": "go.mod (google.golang.org/grpc v1.77.0); core/agentmgr/python_agent.go:431 call path", "violation": "govulncheck reports GO-2026-6061 (xDS RBAC / HTTP2 transport vulnerabilities in grpc) with call-graph reachability from agentmgr.Stop; fixed in grpc v1.82.1", "rule": "go-manifesto section 12 (govulncheck gate fails closed on a new CVE)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: grpc bumped v1.77.0 -> v1.82.1 in gapi and goblin in the same operator-authorized session (goblin's require moved to indirect - it never imported grpc directly), re-vendored, x/net and friends lifted transitively. Artifact: 'mage vuln' reports 'Your code is affected by 0 vulnerabilities' in BOTH repos; both builds green; goblin full suite green under -race (consumer-first check); gapi suite shows zero DATA RACE and only the pre-existing GAPI-DIV-021 failures.", "opened": "2026-07-27"}
{"id": "GAPI-DIV-020", "surface": "core/eventbus (TestUnsubscribePrefix, TestSubscribePrefixWithContext_*), core/transport (TestQUICHandshake)", "violation": "Six data races detected by the newly enabled -race test gate (races existed before; the gate now surfaces them)", "rule": "go-manifesto section 12 (tests run with -race); agent ruleset (flaky behavior is a defect)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: two root causes. (1) transport: acceptLoop read the mutable q.listener field unlocked while Close() nil-ed it under q.mu; the loop now takes the listener as an argument and never touches the field. (2) eventbus: the five races were in the tests, not the bus (dispatch is correctly locked and handlers are async by contract) - test counters written from handler goroutines were plain ints read by the test main; converted to atomic.Int32. Artifact: 'go test -race -count=10 ./core/eventbus/... ./core/transport/...' green; full 'mage test' shows zero DATA RACE reports (remaining failures are the separately tracked GAPI-DIV-019 and GAPI-DIV-021).", "opened": "2026-07-27"}
{"id": "GAPI-DIV-021", "surface": "test/adk (TestTimerAgent_Execution - NOTE: earlier revisions of this entry mislocated it in core/agentmgr)", "violation": "One remaining pre-existing failure: the ADK harness's gapid discovers the repo checkout's own agents/ directory (USER search path), and its example agents (cron_hourly, one with a broken id) each burn ~10s start timeouts, pushing simple_timer's transitions past the test's windows (~90s failure). RESOLVED as part of the agentmgr cluster work: TestGoAgent_Describe (Go/Python describe schema parity), the core/agentmgr package 10m timeout (GoAgent Start/Stop held the write lock across publishStatus RLock and cmd.Wait - guaranteed self-deadlock; ported PythonAgent's lock discipline), Stop misreporting our own SIGTERM as an error, and TestCrossADKParity (green after the schema fix)", "rule": "go-manifesto section 4 (Test is the suite CI runs; it gates green)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-27: four stacked causes. (1) Harness pollution: RUNTIME_AGENT_PATH now fenced to the fixtures dir (framework.go deliberately appended the checkout's agents/); lifecycle tests repointed at the fixture agent test_simple_service. (2) Discovery routing bug: the timer branch matched only '.py' so every '.py.timer' fixture became a Python SERVICE whose runner awaits a readiness signal timers never send; branch now accepts '.py.timer'. (3) TimerAgent published no lifecycle status at all (SetRunID was a no-op, the bus was dropped), so controller starts always timed out; it now stores the run id and publishes RUNNING/STOPPED like its siblings (unit test TestTimerAgent_ControllerStart). (4) Orphaned agent children inherited the test binary's stdout past Process.Kill and tripped go test's I/O watchdog; the harness now kills the process group (Setpgid). Artifact: 'mage test' FULLY GREEN under -race - 13 packages ok, zero FAIL, zero DATA RACE - the suite's first-ever complete pass.", "opened": "2026-07-27"}
{"id": "GAPI-DIV-025", "surface": "CI (Full Build and Test job) - test/adk cross-ADK suite", "violation": "TestCrossADKParity Python subtests (DescribeMetadata, CapabilityDetection) fail on the GitHub runner while green locally. ROOT CAUSE CONFIRMED 2026-07-28 and reproduced hermetically: *.so is gitignored and no CI step ever ran 'mage python:build', so the runner has no gapi/native/_adk.so. gopy's generated gapi/native/adk.py chdirs into its own directory so dlopen can find that extension and restores the caller's directory only on the success path - the ImportError escapes with the process parked in adk/python/gapi/native, and every relative path handed to runner.py afterwards resolves against the wrong root ('FileNotFoundError ... /adk/python/gapi/native/fixtures/python/simple_service.py', exit 2). Locally the extension exists, so the bug never fires.", "rule": "go-manifesto section 12 (the suite passes where the gate runs); ecosystem-manifesto (CI is a consumer like any other)", "owner": "kernel", "status": "resolved", "exit": "TWO fixes landed 2026-07-28: (1) runner.py restores its entry cwd in a finally around the native import, so a missing extension degrades to DummyAdk instead of corrupting global process state - pinned by test/adk/runner_cwd_test.go, which stages the ADK tree without any .so and asserts --describe still parses; (2) both CI jobs build the extension with 'mage python:build' before running tests, and the build job raises net.core.rmem_max/wmem_max for the QUIC control plane. RESOLVED 2026-07-28: the remaining artifact was obtained - CI run 30388150945 at commit 123c8d2 is green on main across all three jobs ('No-Replace Module Resolution' 1m39s, 'Full Build and Test' 4m48s, 'Execute Cross-ADK Tests' 2m48s). The TestPythonAgent_Reload symptom recorded here on 2026-07-28 is a distinct defect and moved to GAPI-DIV-028.", "opened": "2026-07-28"}
{"id": "GAPI-DIV-026", "surface": "core/agentmgr/python_agent.go", "violation": "PythonAgent has no unexpected-exit watcher: a service process that dies without Stop (crash, external signal) is never reaped or reported, leaving a live state over a dead process - the same gap fixed for GoAgent in phase 2c", "rule": "ecosystem-manifesto (fail loudly at boundaries); cross-ADK behavior parity", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: PythonAgent mirrors the GoAgent exit watcher - waitOnce-serialized Wait in Start for service agents, FAILED published for exits Stop did not initiate, slot reaped, plus a Pid() accessor for parity; regression tests TestPythonAgent_UnexpectedExitReported / TestPythonAgent_StopDoesNotDoubleReportFailure. (was: Mirror GoAgent's exit watcher: waitOnce-serialized Wait in Start for service agents, FAILED published for exits Stop did not initiate, slot reaped.)", "opened": "2026-07-28"}
{"id": "GAPI-DIV-027", "surface": "runtime boot (cmd/gapid; no core/pid1 exists)", "violation": "gapi-architecture.md presents GAPI as 'PID 1 capable' with a core/pid1 package, PID-1 boot phases, and non-negotiable orphan reaping; no pid1, subreaper, or orphan-reaping code exists anywhere in the source - gapid cannot run as init", "rule": "gapi-architecture.md (PID 1 capable; core/pid1; boot phases); silo rule: design docs describe the target, the ledger tracks reality", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: the PID-1 obligations exist and are container-proven - core/subreaper (prctl + SIGCHLD reap loop feeding agentmgr.NotifyExited), core/pid1 (explicit six-signal semantics, no implicit defaults), core/mounts (ordered early-mount executor, idempotent, --no-early-mounts), core/shutdown (StopAll -> sync -> reverse umount -> reboot(2) behind an asserted syscall interface), core/watchdog (keepalive + magic close), kmsg progressive logging, and the Phase 0-2 boot runner behind gapid --pid1 (supervisor.go split under the line limit in the same motion; gapictl shutdown --reboot/--halt added). Proof: mage testPid1 runs gapid as PID 1 of a rootless podman container - orphaned grandchild reaped with true wait status, SIGTERM caught by the explicit handler, graceful teardown, exit 0. (was: Either implement the PID-1 obligations (subreaper/init signal semantics, orphan reaping loop, boot phases) behind an explicit init mode, or amend the design doc to mark PID 1 capability as future scope.)", "opened": "2026-07-28"}
{"id": "GAPI-DIV-028", "surface": "agent process lifetime (core/lifecycle/controller.go, core/agentmgr/go_agent.go, core/agentmgr/python_agent.go)", "violation": "Every agent process gapid started was SIGKILLed roughly one millisecond after the supervisor declared it running. lifecycle.Controller.ApplyWithContext derived a start-scoped context (context.WithTimeout(ctx, c.WaitStart)) and both GoAgent.Start and PythonAgent.Start spawned the child with exec.CommandContext against it. os/exec installs a watchdog goroutine that calls Process.Kill on that context's completion, so the agent died the moment the start operation finished - the deferred cancel is function-scoped, not block-scoped, so it fired on ApplyWithContext's return. Downstream the only evidence was a later verb failing with 'agent not running', which is how this presented: TestPythonAgent_Reload reading state 'failed'. Originally recorded on 2026-07-28 as a CI-only, green-locally flake with an unexplained fixture death; that characterisation was wrong on both counts. The kill is deterministic and reproduces locally when the test is run in isolation against a freshly built gapid - the earlier 'green locally' readings came from a full-package run whose ordering masked it and, at least once, from a stale bin/gapid that predated the source under test.", "rule": "go-manifesto section 12 (the suite passes where the gate runs); ecosystem-manifesto (CI is a consumer like any other)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: GoAgent.Start and PythonAgent.Start now spawn with exec.Command; the process is owned by Stop, which sends SIGTERM and escalates on its own schedule. ApplyWithContext still derives a start-scoped context, but cancels it explicitly when Start returns instead of deferring to function exit, and lifecycle.Runner.Start now documents the contract the bug violated: the context bounds the start call and an implementation must not bind a spawned process to it. The reload path's exec.CommandContext at python_agent.go is deliberately left alone - that command is a synchronous cmd.Run whose lifetime IS the request. Artifacts: core/agentmgr/start_context_test.go asserts an agent keeps writing heartbeats after its start context is cancelled, and was confirmed to FAIL against the pre-fix code ('4 beats before, 4 after') and pass after; TestPythonAgent_Reload passes in isolation for the first time; full race suite green including test/adk (117.2s) and test/e2e (10.9s), with zero 'agent process exited unexpectedly' warnings where CI had seven. NOTE for the record: this defect predates the GAPI-DIV-026 exit watcher. Before the watcher the death was silent and the suite passed on a stale 'running' state - the whole ADK suite was green while every agent it started was being killed. The watcher did not break CI; it made a pre-existing breakage visible, which is exactly what it was for.", "opened": "2026-07-28"}
{"id": "GAPI-DIV-029", "surface": "adk/python/gapi/native/ (committed gopy output)", "violation": "The generated python bindings are committed, but 'mage python:build' emits them in a different order every run: gopy iterates a map when writing adk.go/adk.py, so a no-op regeneration rewrites the exported-function blocks in a fresh permutation. Every developer who runs mage testADK or mage python:build dirties the working tree with a diff that changes nothing, and no reviewer can tell a real binding change from a shuffle. Surfaced 2026-07-28 while wiring the CI native-extension build (GAPI-DIV-025).", "rule": "go-manifesto (non-determinism must be identified and bounded; flaky behavior is a defect); build-environment.md (hermetic, reproducible builds)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29 by commit 14c535d 'build(adk): remove the generated bindings from the index'. This entry was FALSELY CLOSED once before, on a .gitignore addition - which has no effect on files already tracked, so the seven blobs stayed in the tree and the churn stayed live. It was reopened, and the close is now verified AFTER the commit rather than before, which is the discipline whose absence caused the first false close. ARTIFACTS, in order of strength: (1) 'git ls-tree -r HEAD -- adk/python/gapi/' lists only the four hand-written modules (__init__, agent, protocols, schemas) and nothing under native/. (2) Every artifact the generator emits - adk.c, adk.go, adk.py, adk_go.h, adk_go.so, _adk.so, build.py, go.py, __init__.py, __pycache__ - reports as ignored and none as tracked. (3) The round trip: deleting adk/python/gapi/native/ entirely and running 'mage python:build' regenerates it and leaves 'git status' clean, so the removal costs a fresh clone nothing and cannot reintroduce churn. (4) 'mage testADK' passes on the regenerated bindings, including TestCrossADKParity, so the Python ADK is exercised for real rather than through its DummyAdk fallback.", "opened": "2026-07-28"}
{"id": "GAPI-DIV-030", "surface": ".github/workflows/ci.yml (Full Build and Test job)", "violation": "magelib has exported a govulncheck target since the build library landed, and Magefile.go registers it as Vuln, but no workflow ever invoked it. The CVE gate existed on paper only, so dependency drift accumulated as 15 open GitHub Dependabot alerts (13 golang.org/x/crypto, 1 golang.org/x/net, 1 github.com/quic-go/quic-go) that no build ever reported. The call-graph scanner also saw one finding Dependabot never raised at all - GO-2026-5970, an infinite loop on invalid input in golang.org/x/text - which was package-reachable. Reported by the operator from the GitHub UI, not by the build.", "rule": "go-manifesto (assume hostile inputs; a gate that never runs is not a gate); build-environment.md (the Magefile is the single entry point CI drives)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: CI now runs 'mage vuln' between the build and the test suite, so a reachable CVE fails the build instead of accruing as an alert. Triage on 2026-07-28 found none of the alerts call-reachable: every x/crypto CVE lives in golang.org/x/crypto/ssh or ssh/agent, and the build graph imports only chacha20poly1305, curve25519, hkdf, pbkdf2 and scrypt; CVE-2026-25680 is in x/net/html against a graph that imports x/net/ipv4, ipv6 and bpf; CVE-2026-40898 is HTTP/3 QPACK trailer expansion in quic-go/http3, and the control plane speaks raw QUIC streams demuxed by ALPN, never http3. The alerts were still wrong to be sitting open: version-range scanners cannot see reachability, so an unpatched floor is indistinguishable from an exploitable one to anyone reading the repo from outside. Bumped to clear the floors: golang.org/x/crypto 0.50.0 -> 0.53.0 (0.52.0 is the advisory floor; 0.53.0 is what x/net 0.56.0 demands), golang.org/x/net 0.53.0 -> 0.56.0, golang.org/x/text 0.36.0 -> 0.39.0, github.com/klauspost/compress 1.17.9 -> 1.18.7, and github.com/quic-go/quic-go 0.57.1 -> 0.59.1. The quic-go jump crosses two minor versions on the module the shared listener is built on and needed no source change. Remaining and permanent: GO-2026-5932 (golang.org/x/crypto/openpgp is unmaintained) has no fixed version, so it stays a module-level finding for as long as anything in the graph requires x/crypto. It is not imported, and govulncheck's call-graph filter is what keeps it from failing the gate - which is the reason the gate is govulncheck and not a version-range scanner. Artifacts: govulncheck reports 0 called and 0 imported vulnerabilities (was 1 imported, 24 in required modules); lint 0 issues / 0 nosec; 'go test -race ./...' green across all 22 packages including test/adk (113.8s) and test/e2e (11.6s); vendored GOWORK=off build OK.", "opened": "2026-07-28"}
{"id": "GAPI-DIV-031", "surface": "Magefile.go python:build target (go build -buildmode=c-shared step)", "violation": "The c-shared build hardcoded 'go build -mod=mod', which go rejects in workspace mode ('-mod may only be set to readonly or vendor when in workspace mode, but it is set to \"mod\"'). 'mage python:build' therefore FAILED in the dev shell for every developer with go.work active, and passed in CI only because ci.yml sets GOWORK: \"off\" globally at lines 17 and 83. This is GAPI-DIV-025 inverted: that one was CI-red and locally green, this one is locally red and CI-green. Surfaced 2026-07-28 while verifying that the bindings regenerate cleanly for GAPI-DIV-029.", "rule": "build-environment.md (the dev shell and CI build the same way); go-manifesto section 12 (the suite passes where the gate runs); ecosystem-manifesto (CI is a consumer like any other)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-28: the -mod flag was dropped entirely. The default (readonly) is correct in both modes - go.work resolves the sibling modules in the dev shell, and vendor/ satisfies the CI path - so the target no longer depends on GOWORK. Artifact: with adk/python/gapi/native wiped first, 'nix develop -c mage python:build' now completes in workspace mode (previously failed at the go build step) AND 'GOWORK=off nix develop -c mage python:build' still completes, each producing the full artifact set including adk.c, adk_go.h, adk_go.so and _adk.so. NOTE: the stale committed header _adk.cpython-313-x86_64-linux-gnu.h is NOT reproduced by either run - the current toolchain does not emit it - which is recorded against GAPI-DIV-029.", "opened": "2026-07-28"}
{"id": "GAPI-DIV-032", "surface": "core/crypto/verifybinary.go and pkg/cli/security.go (agent signature chain)", "violation": "Signature verification could never succeed. 'gapictl crypto sign' signed the CANONICAL digest - []byte(crypto.HashFile(file)), a bare hex string (pkg/cli/security.go:38) - while VerifySignedBinary verified against the raw bytes of the .b3 sidecar (core/crypto/verifybinary.go:43), which Magefile.go:64 writes as hexSum+\"\\n\". The two messages differed by one trailing newline, so no signature the shipped CLI produced could ever verify. The same function trimmed for the digest comparison and did NOT trim for the signature check, which is what hid it: 'if actual != strings.TrimSpace(string(hashData))' immediately above 'if !Verify(pub, hashData, sigBytes)'. Because core/agentmgr/discovery.go:349-357 gates agent start on this in production mode, the practical effect was that --production could not start ANY signed agent. Found by the 2026-07-29 documentation audit while checking whether the documented signing workflow actually worked.", "rule": "go-manifesto (test behavior at public interfaces; assume hostile inputs, use proven crypto only); ecosystem-manifesto provenance rule (one verification codepath)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29: verification now covers the CANONICAL digest - Verify(pub, []byte(actual), sigBytes) - matching what the signer covers. Signing the digest rather than the sidecar's bytes is the sounder contract: it keeps the chain pubkey -> digest -> binary independent of how the sidecar is spelled, so a .b3 rewritten without a trailing newline still verifies against an unchanged binary. The doc comment, which had described the old (broken) intent as signing 'the .b3 file's bytes', was corrected to match. ROOT CAUSE OF THE GAP IN COVERAGE: no test ever crossed the two paths. Any test that signed and verified through a single helper would have passed against the broken code, because both halves would have used the same message. The new core/crypto/verifybinary_test.go therefore reproduces the CLI's signing form and the build's sidecar format separately, then verifies through the discovery path. Artifacts: six new tests pass; and, decisively, reverting the one-line fix makes three of them FAIL with the exact symptom ('a signature made the way the CLI makes them did not verify', 'a signature over the sidecar bytes was accepted') - confirmed by experiment, then restored. One test pins the contract against future drift by asserting that a signature over the sidecar bytes is REJECTED. Full gate: 'mage lint' 0 issues / 0 nosec, 'mage test' green. HOW IT SHIPPED WITH A GREEN SUITE - the most transferable part: production-mode signature acceptance WAS covered, by core/agentmgr/safety_test.go TestSafeToExecute_ProductionAcceptsValidSignature. It passed because its helper signBinary signed []byte(hash+\"\\n\") - the sidecar bytes - which is what the verifier happened to check, and its own comment described that as 'the gapictl agent verify convention'. The test mirrored the IMPLEMENTATION rather than the CONTRACT, so it stayed green while the shipped CLI produced signatures nothing could verify. Fixing the verifier made that test fail, which is how the mirroring was found. signBinary now signs what 'gapictl crypto sign' signs, and its comment records why. The lesson generalises: a helper that constructs its own input by copying the code under test cannot detect a contract mismatch, however many assertions follow it. All eight safety tests pass with the corrected helper, including the tampered-binary and wrong-key negatives, so the contract was tightened rather than loosened.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-033", "surface": "nix/module.nix and nix/package.nix", "violation": "The Nix deployment path could not work. nix/package.nix set buildFlags = [\"-mod=mod\"] while src carries the committed go.work (use ( . ../magelib )) without its sibling: go entered workspace mode, where -mod is illegal, so the derivation failed and the package had NEVER been built. nix/module.nix compounded it: ExecStart passed 'gapid -config <file>', a flag cmd/gapid/gapid.go does not define and cobra rejects, so the unit failed to start whenever configFile was set; the unit environment set GAPI_AGENTS_DIR, which no code reads (core/config/agent_paths.go reads RUNTIME_AGENT_PATH), making services.gapi.agentsDir a no-op whose default was not even on the search path; the generated config used certFile/keyFile where core/config.TransportConfig expects tlsCert/tlsKey, so viper dropped them and the daemon silently generated a throwaway self-signed certificate instead of loading the configured one; and listenAddress defaulted to 127.0.0.1:4242 where the runtime default is :14242. Found by the 2026-07-29 documentation audit; the same class as GOBLIN-DIV-030, which had already been fixed in the sibling repo.", "rule": "build-environment.md (the packaged artifact is a consumer like any other); ecosystem-manifesto section 7; go-manifesto (fail loudly at boundaries - a silently ignored TLS key is a downgrade, not a default)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29: package.nix sets env.GOWORK = \"off\" and drops the -mod flag entirely (the default readonly is correct); module.nix drops the nonexistent -config flag and passes the real override RUNTIME_CONFIG as an environment variable, renames GAPI_AGENTS_DIR to RUNTIME_AGENT_PATH, spells the TLS keys tlsCert/tlsKey, and defaults listenAddress to 127.0.0.1:14242. The stale cleanSource comment claiming it 'respects .gitignore' was corrected - it filters VCS and editor files, and vendor/ is tracked. Artifact: 'nix build' of nix/package.nix reaches installPhase and emits bin/gapid and bin/gapictl, where it previously failed in buildPhase; the resulting binary reports a real version (see GAPI-DIV-007). The module is exercised end to end by the VM check added with the flake outputs.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-034", "surface": "ENABLED agent metadata (core/agentmgr/discovery.go, the three runners, core/supervisor/supervisor.go)", "violation": "ENABLED was parsed and discarded. The agent-level Enabled field had ZERO consumers: decoded at core/agentmgr/discovery.go, declared on core/adk/meta/meta.go, and never carried onto a constructed agent - which is why Describe() had no key for it and the supervisor's desc map could not consult it. core/supervisor/supervisor.go admitted this in a comment ('We should verify enabled? implicit enabled for now.'). Meanwhile docs/configuration.md, docs/features.md and docs/agent-examples.md all told operators that ENABLED = False prevents an agent from loading. It did nothing. Found by the 2026-07-29 documentation audit.", "rule": "go-manifesto (explicit over clever; fail loudly at boundaries); ecosystem-manifesto (documented behaviour is a contract)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29 by honouring it (operator decision; the alternative was removing it from the schema and the docs). Plumbed across four seams: discovery resolves it, each runner carries it with SetEnabled/Enabled accessors set post-construction through the enabledSetter interface - matching the RunIDSetter idiom rather than adding a fourth positional argument to three already-long constructors - and the supervisor skips the three auto-start paths for a disabled agent. SEMANTICS: systemd's. A disabled agent is still discovered, registered and visible to 'gapictl agent status', and can still be started explicitly through the lifecycle verbs; only automatic start is skipped. THE DANGEROUS PART WAS THE DEFAULT, and getting it wrong would have been catastrophic: Go agents do not emit an 'enabled' key at all (verified against test/adk/fixtures/go/hash_agent/main.go, whose describe carries only id/type/version/language/description/capabilities), so a plain bool would have unmarshalled their silence as false and DISABLED EVERY GO AGENT IN THE SYSTEM. The field is therefore *bool: nil means absent means enabled, and only an explicit \"enabled\": false disables. agentmgr.AgentEnabled treats any type not carrying the flag as enabled, so a future runner cannot become silently un-startable. A second, self-inflicted trap was caught by the tests: the struct comment claimed a default of true while relying on a bool zero value, which is false; every constructor now sets it explicitly. Artifacts: four new tests in core/agentmgr/enabled_test.go covering absent-means-enabled, explicit-false-disables, all three runner types defaulting to enabled and honouring SetEnabled, and the unknown-type fallback; 'mage lint' 0 issues / 0 nosec; 'mage test' green under -race.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-035", "surface": "flake.nix outputs, nix/checks.nix, nix/tests/, nix/generators/base.nix", "violation": "flake.nix exposed devShells.default and nothing else. nix/module.nix was therefore unreachable by any consumer (no nixosModules output), nix/package.nix was unbuildable through the flake (no packages output), 'nix flake check' ran nothing (no checks output), and nix/generators/base.nix plus nix/generators/README.md documented 'nix build .#iso', '.#vm', '.#qcow', '.#lxc' and five more formats that could not evaluate - there was no nixos-generators input and no packages output at all. docs/installation.md and docs/pid1-testing.md both instructed operators to use those targets. Separately, base.nix's two example agents used COMMENT-style metadata ('# ENABLED = True', '# TYPE = timer'), which agent discovery reads via getattr on module attributes and therefore drops silently: every generated image would have shipped a 'timer' that never registered as one.", "rule": "build-environment.md (the packaged artifact is a consumer like any other); ecosystem-manifesto section 7 (every repo's gates are runnable); go-manifesto section 12", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29 by implementing the outputs rather than deleting the documentation (operator decision). flake.nix now exposes nixosModules (default and gapi) OUTSIDE flake-utils.eachSystem - a NixOS module is evaluated by the consuming system's module system, so nesting it would produce nixosModules.<system>.default, which no consumer can import - plus packages.default/gapi, the nine nixos-generators image formats the README already advertised, and checks.<system>. base.nix's example agents were rewritten as real module-level assignments so the images ship agents that actually register. The module also now installs its own binaries: services.gapi.enable previously left an operator with a running daemon and no gapictl to talk to it, where goblin's module already did this. THE SYSTEM LIST HAD TO CHANGE: adding the nixos-generators input re-locked nixpkgs to 26.11, which has DROPPED x86_64-darwin, and flake-utils.eachDefaultSystem enumerates it - merely instantiating pkgs for that platform now throws. Replaced with an explicit list of x86_64-linux, aarch64-linux and aarch64-darwin, which is honest for a product whose own nix/package.nix declares platforms.linux. NOTE FOR GOBLIN: it still uses eachDefaultSystem with an older lock and will hit the same failure on its next input update. Artifacts: 'nix flake show' lists all four output classes; 'nix flake check' passes; 'nix build .#packages.x86_64-linux.lxc' produces a 245 MB bootable image containing 53 gapi store paths; and the new checks.x86_64-linux.module-boot VM test boots the module and passes in 15.11s, asserting against a RUNNING system that ExecStart carries no -config flag, that RUNTIME_AGENT_PATH is set and GAPI_AGENTS_DIR is not, that 'gapid version' reports 'Runtime Core: 0.1.0' rather than the dev placeholder, and that the generated config uses tlsCert rather than the silently-dropped certFile - which is the end-to-end regression test for GAPI-DIV-033 and GAPI-DIV-007.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-036", "surface": "core/agentmgr/schedule.go parseSystemdSchedule", "violation": "All three systemd-style timer prefixes are aliases for the same behaviour. parseSystemdSchedule matches OnUnitActiveSec=, OnBootSec= and OnStartupSec=, strips whichever prefix matched, parses the remaining duration, and returns an IntervalSchedule in every case - the prefix itself is discarded. So OnBootSec=1m does not run once, one minute after boot; it runs every minute forever, which is what OnUnitActiveSec=1m means. OnStartupSec is the same. The doc comment on ParseSchedule and docs/features.md both described the systemd semantics, so an operator declaring SCHEDULE = \"OnBootSec=1h\" for a once-per-boot task got an hourly job instead - a silent behavioural difference, not an error. Discovery's own default for a timer with no SCHEDULE is OnUnitActiveSec=60s, which is the only spelling whose name currently matches what it does.", "rule": "go-project-manifesto section 4 (deterministic behaviour by default; an interface that accepts a name must honour it or reject it); cooperative-ecosystem-manifesto section 3 (systemd-compatible spellings carry systemd semantics or are not offered)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29 by implementing the two missing semantics rather than rejecting the prefixes, so a systemd-compatible spelling carries systemd behaviour. A new OnceSchedule fires a single time at a fixed instant and then returns the ZERO time; the Schedule interface documents zero as 'exhausted' and TimerAgent.run stops on it instead of busy-waiting. OnStartupSec anchors to the parse instant, OnBootSec to the system boot, read from unix.Sysinfo rather than /proc/uptime so it works before the PID-1 early mount phase; non-Linux builds return time.Now, degrading OnBootSec to OnStartupSec, which is announced in the doc comment rather than silent. The prefix table now carries each prefix's anchor as data, so adding one without deciding its anchor is a compile-time hole rather than a silent alias - that omission is what produced the bug. A missed elapse point fires immediately, once (the run loop clamps a negative delay to zero), matching systemd's monotonic-timer behaviour rather than cancelling. ParseScheduleAt takes now and boot injected so the relative forms are testable without waiting on a real clock. ARTIFACTS: core/agentmgr/schedule_semantics_test.go asserts fire COUNT and fire INSTANT per prefix - OnUnitActiveSec repeats, the other two fire exactly once, and OnBootSec=45m resolves to 15m from a now that is 30m past boot, proving the anchor. End to end, test_timer.sh runs an OnStartupSec=3s agent for 11s and requires EXACTLY one fire plus a 'timer schedule exhausted' log line. Flipping the two prefixes back to repeating makes both the unit tests and the harness fail ('OnStartupSec=3s fired 3 times in 11s, want exactly 1').", "opened": "2026-07-29"}
{"id": "GAPI-DIV-037", "surface": "core/agentmgr/discovery.go agent construction, core/agentmgr/timer_agent.go, pkg/cli/templates/go_timer.tmpl", "violation": "Only PYTHON timers are scheduled. discovery.go routes to NewTimerAgent when meta.Type == \"timer\" AND the path ends in .py or .py.timer; every other timer falls through to NewGoAgent, which contains no reference to a schedule at all. TimerAgent.execute confirms the asymmetry - it always spawns ta.pyRunner, so the scheduler has no way to fire a binary. The supervisor's timer auto-start path keys on desc[\"type\"] == \"timer\" and calls ActionStart, so a Go timer starts exactly once, at discovery, and never fires again; its declared SCHEDULE is read into the describe map and then dropped. gapictl agent new <name> --type timer defaults to Go and scaffolds precisely this: pkg/cli/templates/go_timer.tmpl emits \"schedule\": \"OnUnitActiveSec=60s\" in its metadata and a start() that prints \"Timer triggered\". The scaffolded agent therefore appears to work - it runs once - while never honouring the interval it advertises. Separately, TimerAgent.execute bounds each fire with a hardcoded 30-second context timeout, undocumented anywhere: a scheduled backup or report that runs longer is SIGKILLed mid-write with only a 'timer execution failed' log line.", "rule": "go-project-manifesto section 4 (deterministic behaviour by default); section 6 (behaviour at the public interface is the contract - agent new is a public interface and must not scaffold a broken agent); cooperative-ecosystem-manifesto section 2 (the two ADKs have identical semantics)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29 by giving binary agents the same scheduling Python ones have, rather than rejecting TYPE=timer on a non-Python agent - the dual-ADK parity claim is load-bearing and worth keeping. Scheduling was never language-specific; only the FIRE was. TimerAgent gained a lang field and a fireCommand seam: Python modules go through the ADK runner, an executable is run directly, which is exactly what its main does when not asked to --describe. NewBinaryTimerAgent is a separate constructor rather than a widened signature, so existing call sites are untouched and the choice is explicit at the point of construction. Discovery now routes meta.Type == \"timer\" to the scheduler for BOTH ADKs; the isPython test that used to gate it selects the constructor instead. The gapictl go_timer.tmpl needed no change - it already prints and exits, which is the correct shape for a fire. Also fixed while here: execute now derives its timeout context from the run loop's context rather than context.Background, so stopping a timer cancels a fire in flight instead of leaving it to run out the full 30 seconds; that also cleared a gosec G118 the new code surfaced. ARTIFACTS: test_timer.sh builds a real Go timer fixture alongside the Python one and requires both to fire at least four times in an 11-second window - observed 'triggered=11 completed=11 python=5 go=5'. Restoring the isPython gate makes it fail with 'expected at least 8 triggers in 11s across two timers, got 0'.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-038", "surface": "core/config/config.go Load (viper AutomaticEnv + Unmarshal)", "violation": "The RUNTIME_ environment override works for only SOME config keys, and fails SILENTLY for the rest. Load registers viper.SetDefault for transport.type/address/insecureSkipVerify, metrics.*, logging.* and timeouts.*, then relies on AutomaticEnv plus Unmarshal. viper's Unmarshal does not consult AutomaticEnv for a key it has never seen, so every key WITHOUT a registered default is unreachable from the environment. Empirically, with all six variables exported: RUNTIME_LOGGING_LEVEL and RUNTIME_METRICS_ENABLED apply, while RUNTIME_SUPERVISOR_PID1MODE, RUNTIME_SUPERVISOR_PRODUCTIONMODE, RUNTIME_SECURITY_VERIFYKEY and RUNTIME_TRANSPORT_TLSCERT are all dropped and the fields keep their zero values. The affected set is every supervisor.* key (productionMode, pid1Mode, noEarlyMounts, watchdog.*, shutdown.*), security.verifyKey, and transport.tlsCert/tlsKey/tlsCa. This is a security-relevant silent downgrade in both directions: an operator who sets RUNTIME_SUPERVISOR_PRODUCTIONMODE=true gets a daemon running WITHOUT production mode and without signature enforcement, and no error; one who sets RUNTIME_TRANSPORT_TLSCERT gets the auto-generated self-signed certificate. security.verifyKey happens to survive only because supervisor.go carries a separate os.Getenv(\"RUNTIME_VERIFY_KEY\") fallback under a different variable name. docs/configuration.md, docs/config-example.md and nix/module.nix all state or assume that any key can be set from the environment.", "rule": "go-project-manifesto section 4 (deterministic behaviour by default; no hidden global state); section 9 (fail loudly at boundaries - a rejected override is a boundary failure and must not be silent); cooperative-ecosystem-manifesto section 5 (configuration surfaces are uniform or explicitly documented as partial)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29 by deriving the bindings from the Config struct rather than listing keys - the gap was exactly the keys nobody thought to list, so a list would reproduce it. bindEnvOverrides walks the tree by mapstructure tag and calls BindEnv on every scalar leaf, passing the environment variable name EXPLICITLY (EnvKeyFor) so the binding does not depend on how viper composes a prefix with a key replacer. Maps are skipped and documented as config-file only: logging.loki.labels has no single-variable spelling. A field added later is reachable because it exists. Load also moved off viper's package-level singleton to a private viper.New() instance: the global accumulated bindings and defaults across every call in a process, which is the hidden global state that makes a configuration bug reproduce only on the second test - TestLoadDoesNotLeakBetweenCalls pins that. ARTIFACTS: core/config/envoverride_test.go walks Config by reflection and drives 32 leaves, one subtest each, asserting the loaded value equals a probe distinguishable from both the zero value and the registered default (insecureSkipVerify and file.compress default true, so they are probed with false - probing true would pass against a broken binding). Removing the bindEnvOverrides call fails transport.tlsCert, .tlsKey, .tlsCa, security.verifyKey, logging.loki.url and every supervisor.* key with 'this key is not reachable from the environment'. Precedence is separately asserted as environment over file over default, since binding every key could have inverted it, and the security-relevant keys are also asserted by name so the intent survives a refactor of the walk. Note logging.loki.url was ALSO unreachable and appears in no earlier analysis - the reflection walk found it, which is the argument for the walk.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-039", "surface": "test/test_timer.sh, agents/python/timers/tick.py.timer", "violation": "'mage testTimer' is red and has been. Three independent defects in the harness, none of which the exit code distinguishes. (1) The script hardcodes absolute /nix/store paths into PATH, pinning go-1.25.4 while the dev shell now pins Go 1.26 - a hermeticity violation of exactly the kind 'mage doctor' exists to catch, and it rots on every toolchain bump. (2) It exports RUNTIME_AGENTS_DIR, which is read by NOTHING; the discovery variable is RUNTIME_AGENT_PATH (core/config/agent_paths.go). The agents directory is therefore found only incidentally through the dev-build search paths, or not at all. (3) The config it generates uses transport.certFile and transport.keyFile, the silently-dropped spellings - the same defect corrected in config/config.yaml - so the harness configures no TLS and the daemon falls back to a generated self-signed certificate. Separately, agents/python/timers/tick.py.timer declares no ID, so it registers under the literal agent id \"unknown\". Observed behaviour with RUNTIME_AGENT_PATH set correctly and only tick.py.timer on the path: SCHEDULE=OnUnitActiveSec=5s produced ONE fire in 13 seconds where the interval implies two, and the log carries a 'timer agent stopped' for it; across the full fixture directory every timer logs 'timer agent started' TWICE per supervisor boot. The scheduler is not simply idle - something is stopping and restarting timers, and the harness's 15-second window cannot tell that apart from a schedule that never fired.", "rule": "go-project-manifesto section 11 (gates are hermetic and runnable from a clean shell); section 6 (a test asserts behaviour at the public interface, and a green/red signal must mean one thing); build-environment.md (no absolute store paths in checked-in scripts)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29. The ROOT CAUSE was not the harness: TimerAgent.execute invoked the Python runner without --type, so the runner defaulted to \"service\" and took the service path - AgentWrapper.start ran _await_ready, which looks for a live start thread, new non-daemon threads or an open port, found none for a timer body, and spun to its full 20-second deadline; main() then entered 'while True: sleep(0.25)' and never exited. cmd.Run therefore blocked until the 30-second execution deadline and the next fire never came. Measured directly: 'python3 runner.py --module tick.py.timer --start' hung past a 45-second timeout (exit 124); with --type timer it now returns in 0.038s, exit 0. FIX: --type defaults to None and falls back to the module's own TYPE, so an omitted flag cannot silently reclassify a timer as a service; AgentWrapper carries a oneshot flag for timer and oneshot types, runs start() synchronously without the readiness poll (for a one-shot, ready and finished are the same event), and main() skips the supervision loop; TimerAgent.execute passes --id and --type timer. A service still blocks as before (verified: exit 124 under an 8-second timeout). tick.py.timer gained an explicit ID - it had been registering as the literal agent id \"unknown\". test_timer.sh rewritten: inherits PATH from the dev shell, uses RUNTIME_AGENT_PATH plus RUNTIME_SKIP_SYSTEM_AGENTS against a fixture directory it creates, and asserts a FIRE COUNT and that triggers == completions, so a fire that blocks is a failure. Artifacts: 'triggered=5 completed=5 agent-output=5' in an 11s window at a 2s interval; with the supervision loop restored the same test reports 'triggered=1 completed=0' and FAILS, which the old substring assertion passed. CORRECTION to this entry's original text: the doubled 'timer agent started' was two DIFFERENT log lines, one from timer_agent.go and one from supervisor.go, not a double start.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-040", "surface": "pkg/cli/security.go signCmd", "violation": "'gapictl crypto sign' emitted a signature that nothing could verify. It hashed the file, signed the canonical digest, and wrote only <file>.sig - never the <file>.b3 digest sidecar. crypto.VerifySignedBinary reads the .b3 FIRST and returns 'reading digest' before the signature is examined, so a file signed with the documented command failed verification outright, and production-mode discovery rejected it. No other CLI verb writes a .b3 for an arbitrary path either: the Magefile writes them for gapid and gapictl, and 'gapictl agent build' writes them for scaffolded Go agent directories, so an operator signing a hand-built agent binary had no supported way to produce the missing half. Observed: sign a copy of gapictl, then 'gapictl agent verify' reports 'No .b3 file found' while the .sig sits beside it. docs/features.md and docs/getting-started.md both documented sign-then-verify as a complete workflow.", "rule": "go-project-manifesto section 9 (fail loudly at boundaries - emitting half a provenance pair is a silent boundary failure); cooperative-ecosystem-manifesto section 4 (provenance artifacts are produced and consumed through one codepath)", "owner": "kernel", "status": "resolved", "exit": "RESOLVED 2026-07-29. signCmd now writes <file>.b3 as hexSum+\"\\n\" - byte-identical to what Magefile.go emits - before writing the .sig, and reports both paths. Signing is the only operation that already computes the digest, so it is the correct place to record it. Artifact: test/test_integrity.sh signs a freshly built binary agent with nothing but 'gapictl crypto sign', and production-mode discovery starts it while rejecting its unsigned twin; reverting core/crypto/verifybinary.go to the GAPI-DIV-032 behaviour makes that same test fail with 'signed agent did not register'.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-041", "surface": ".github/workflows/ci.yml", "violation": "CI does not run the gates. go-project-manifesto section 'CI is `nix develop --command mage <target>` and nothing else -- lint, build, and test all run through the pinned shell' is not honoured: the workflow runs `nix develop -c go test -v ./...` directly, which DROPS the -race flag that the mage Test target carries, and no job invokes mage lint, mage proto, mage doctor or mage envcheck. The targets themselves are correct and were each closed on that basis (GAPI-DIV-002 for -race, GAPI-DIV-005 for lint/gosec, GAPI-DIV-006 for buf) - those entries are accurate about the targets and say nothing false. The defect is the gap between having a gate and running it: a race regression, a gofmt drift, a new gosec finding or a breaking proto change all merge green. The field guide names this exact failure - 'never invoke go build, pip, or codegen tools by hand where a mage or nox target exists; the targets carry invariants the raw tools do not' - and CI is the caller doing it.", "rule": "go-project-manifesto (CI is mage targets through the pinned shell; tests run with -race; gosec is required not optional); proto-v2-overview phase 2a exit conditions ('go test -race ./... green in both repos', 'the lint/vuln/gosec/buf gates are wired and green')", "owner": "kernel", "status": "open", "exit": "Replace the raw `go test` step with `nix develop -c mage test`, and add jobs for `mage lint` and `mage proto`. `mage vuln` is already wired and is the model. Closing requires a CI run URL showing -race in the test output and a lint job that fails on an introduced gofmt violation - not merely the presence of the steps, since presence is what this entry exists to distinguish from enforcement.", "opened": "2026-07-29"}
{"id": "GAPI-DIV-042", "surface": "test coverage of parsers and wire decoders", "violation": "No fuzz target exists anywhere in the repo. `grep '^func Fuzz' **/*_test.go` returns zero, and there is no testdata/fuzz/ corpus. GAPI-DIV-013 added the Fuzz target and its exit honestly describes it as a 'loud no-op at zero targets', so that entry is correctly closed - this is the work it left. The manifesto requires fuzzing the hostile boundaries: parsers, decoders, anything that eats wire bytes or operator input. Unfuzzed surfaces that qualify: core/schema/validator.go (agent manifest validation, 324 lines), core/agentmgr/schedule.go (systemd-style timer expression parser accepting operator strings), the agent stdout decoder at core/agentmgr/go_agent.go:581 and python_agent.go:610 (JSON from a supervised process into map[string]any), and core/crypto capability-token decode.", "rule": "go-project-manifesto (fuzz the hostile boundaries with native go test -fuzz; seed via f.Add plus a checked-in testdata/fuzz corpus; every crasher minimized and committed)", "owner": "kernel", "status": "open", "exit": "At least one Fuzz target per named surface, seeded with f.Add and a committed testdata/fuzz corpus. `mage fuzz` must stop reporting zero targets. Closing requires the target count in the mage fuzz output, and any crasher found must be minimized and committed as a regression seed rather than only fixed.", "opened": "2026-07-29"}
